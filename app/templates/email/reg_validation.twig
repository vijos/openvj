{% extends "email/extend.twig" %}
{% block body %}
<p>欢迎您加入 OpenVJ，请点击下方的链接完成您的注册：</p>
<p><a href="{{ url("/reg/#{TOKEN}") }}" target="_blank">{{ url("/reg/#{TOKEN}") }}</a></p>
<blockquote>
<p>本邮件有效期为 4 小时，请您尽早完成操作。</p>
<p>如果您 ({{ EMAIL }}) 没有申请在 <a href="{{ url('/') }}" target="_blank">OpenVJ</a> 上注册，请忽略这封邮件。</p>
</blockquote>
{% endblock %}